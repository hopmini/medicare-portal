services:
  identity-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: medicare_identity_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Medicare@2024
    ports:
      - "5433:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
volumes:
  dbdata:
